name: learn-github-actions
on: [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: My first step
        shell: bash
        run: |
          time=$(date)
          echo "::set-output name=time::$time"
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
